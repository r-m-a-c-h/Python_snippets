# Pytest snippets
> pip install pytest

# Run Pytest
> pytest -v 
> python -m pytest -v

# Run Pytest markup
> pytest -m "not slow"

# Pytest report 
> pip install pytest-html
> pytest --html=report.html

# Pytest mock
> pip install pytest-mock